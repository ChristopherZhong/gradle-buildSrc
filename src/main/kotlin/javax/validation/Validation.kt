package javax.validation

object Validation {
    const val GROUP = "javax.validation"
    const val VERSION = "2.0.1.Final"
    // libraries below
    const val API = "validation-api"
}

const val `validation-api` = "${Validation.GROUP}:${Validation.API}:${Validation.VERSION}"
