package javax.inject

object Inject {
    const val GROUP = ""
    const val VERSION = "1"
    // libraries below
    const val INJECT = "javax.inject"
}

const val `javax_inject` = "${Inject.GROUP}:${Inject.INJECT}:${Inject.VERSION}"
