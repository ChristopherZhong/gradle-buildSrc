@file:Suppress("unused", "ObjectPropertyName")

object TruthExtensions {
    const val GROUP = "${Truth.GROUP}.extensions"
}

const val `truth-java8-extension` = "${TruthExtensions.GROUP}:truth-java8-extension"
