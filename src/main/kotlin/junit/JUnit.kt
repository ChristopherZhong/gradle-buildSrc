package junit

object JUnit {
    const val GROUP = "junit"
    const val VERSION = "4.12"
    // libraries below
    const val JUNIT = "junit"
}

const val junit = "${JUnit.GROUP}:${JUnit.JUNIT}:${JUnit.VERSION}"
